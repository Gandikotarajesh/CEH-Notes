#### NSE Scripts
```bash
/usr/share/nmap/scripts/
```
#### Scanning
```bash
nmap -sC -sV -p- -T4 <IP>
```
#### Seveiry scoe or EOL vulnerability
```bash
nmap --script vuln <ip>
or
openvas
```
#### Live Host discovery with fping
```bash
fping -aqg 192.168.0.1/24 
```
#### Live host discovery with netdiscover
```bash
sudo netdiscover -r 192.168.0.1/24 
```
#### Live host discovery with arp-scan
```bash
sudo arp-scan 192.168.0.1/24 
```

#### NMAP Commands
```bash
# Nmap scan if firewall/IDS is opened
nmap -sS -v 10.10.10.16

```
#### NMAP
```bash
# TCP Connect
nmap -sT <ip>

# Syn scan
nmap =sS <ip>

# Version detection
nmap -sV <ip>

# OS Detection
nmap -O <ip>

# UDP scan
nmap -sU <ip>

# Aggressive scan
nmap -A <ip>
```
#### Severity of vulnerability
```bash
1.nmap -Pn - -script vuln <IP> 
2. now copy the CVE number which is vulnerable paste in google and see the value. 
3. The CVE with the least CVSS score is the answer
```
