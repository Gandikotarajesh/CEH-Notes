
#### SMB Enumeration
```bash
nmap -p 139,445 --open -sV <ip>

Anonymous Login
smbclient -L //10.10.55.50 -N
get access to share with anonymous login
smbclient //10.48.183.82/Anonymous -N
get file.txt

Lists all SMB shares on the server 
smbclient -L //10.10.55.50 -U username
 
Connect to specific share
smbclient //10.10.55.50/Public -U username
ls
->Connects directly to the Public share.
-> Once connected, you'll get the SMB prompt ( smb: \> )

Brute-force SMB Credentials: 
hydra -L user_list.txt -P password_list.txt <ip> smb


Retrieve and Read file.txt: 
get file.txt
cat file.txt

------------
Network file shares
we can use smb_enum_shares script
Logged in users details
smb_enum_users
nmap -p 445 --script smb_enum_users --script-args smbusername=admin smbpassword=admin123 <ip>
Workgroups
nmap -p 445 --script smb_enum_groups --script-args smbusername=admin smbpassword=admin123 <ip>

```
#### SMB
```bash
smbclient -L <TARGET_IP> -N
smbclient -L <TARGET_IP> -U <USER>
smbclient //<TARGET_IP>/<USER> -U <USER>
smbclient //<TARGET_IP>/admin -U admin
smbclient //<TARGET_IP>/public -N #NULL Session
## SMBCLIENT
smbclient //<TARGET_IP>/share_name
help
ls
get <filename>
```
#### Directory search
```bash
Directory traversal after dirsearch or gobuster or dirb or dirbuster
dirsearch -u https://example.com
or 
dirsearch -e php,html,js,txt -u https://example.com -> for extension search
or
dirsearch -e php,html,js,txt -u https://example.com -w /usr/share/wordlists/dirb/common.txt -> for wordlist search
```
#### FTP
```bash
nmap -sC -p 21 <ip>
to connect
ftp <ip>
```
#### SNMP
```bash
this protocol used to monitor and manage network devices like PCs, router, Switches, servers
# Tools
Nmap
snmp-check
metasploit
snmp-check <ip>  -> we get all details
snmpwalk -v2c -c public <IP>

Default UDP ports used by snmp
Identify the process running on the system using namp scripts
list valid community strings of the server using nmap scripts (snmp-brute script)
list valid community strings of the server by using snmp_login metasploit module
list all the interfaces of the machine. Use appropriate nmap scripts
```

#### RDP 3389
```bash
check RDP
msfconsole -> rdp_scanner
connect with target
xfreedp /u:admin /p:password /v:<IP>: port

```

#### Nebios
```bash
to enumerate
--script nbstat.nse
```
